#include<bits/stdc++.h>
using namespace std;
#define Faster ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
#define ll long long


void solve()
{
    int n,l,r;
    cin>>n;
    int a[n];
    for(int i=1; i<=n; i++)
        cin>>a[i];
    
    cout<<"1 "<<n<<endl;
}

int main()
{
    Faster;
    int _;      cin>>_;
    while(_--)  solve();
    
    return 0;
}

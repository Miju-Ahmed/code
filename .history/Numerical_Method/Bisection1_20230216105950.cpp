#include<bits/stdc++.h>
using namespace std;

class Bisection()
{
    private:
        double function(double x)
        {
            return x*x*x+x*x-1;
        }
    public:
        void BisectionMethod()
        {
            double pre = 0.001;
            double x0=1,;
            while(function(x))
        }
};
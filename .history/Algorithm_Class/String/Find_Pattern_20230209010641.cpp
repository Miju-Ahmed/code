#include<bits/stdc++.h>
using namespace std;
#define ll long long int
ll mod = 1e9+7;

int main()
{
    int f;  cin>>f;
    while(f--)
    {
        string s,c; cin>>s>>c;
        vector<int>t;
        t.push_back(i+1);
            h2 = ((h2 - s[i]-'a'-1)/base2 + (s[i+c.size()-1]-'a'+1)*base2)%mod;
            base2 = (base2*p)%mod;
        }
        cout<<t.size()<<endl;
        for(int i=0; i<t.size(); i++)
            cout<<t[i]<<" ";
        cout<<endl;
    }
}

/*
miju
nmijuf
*/
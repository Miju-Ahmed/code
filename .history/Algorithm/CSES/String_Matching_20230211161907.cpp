#include<bits/stdc++.h>
using namespace std;
#define ll long long
ll mod = 1e9+1;

int main()
{
    string m,n;
    cin>>n>>m;
    ll  hashm=0, hashn=0,base=31,p=0;
    for(int i=m.size()-1; i>=0; i--)
    {
        hashm = (hashm + (m[i]))
    }
}